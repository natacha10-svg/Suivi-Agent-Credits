import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'suivi-agent-credit',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
